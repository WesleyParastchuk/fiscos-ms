version: "3.9"

services:
  gateway:
    build:
      context: ../../gateway
      dockerfile: docker/Dockerfile.dev
    container_name: fiscos-gateway-dev
    env_file:
      - ../config/.env
    environment:
      - PORT=${GATEWAY_PORT}
    ports:
      - "${GATEWAY_PORT:-3000}:3000"
